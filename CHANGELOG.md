# Changelog

このプロジェクトの主な変更履歴をまとめます。  
形式は [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) を参考にし、バージョニングは [SemVer](https://semver.org/lang/ja/) に準拠します。

---

## [v2.2.4] - 2026-08-03

### Fixed
- ネストしたリストの自動空行挿入位置を修正
  - 従来は、親リスト直後により深い子リストが始まる場合に、**子リスト開始前**へ自動空行が入ることがあった
  - 修正後は、**ネストした子リスト（およびその説明行）が終わって浅い階層のリストへ戻る直前**に自動空行を挿入するよう修正
- 以下のような Markdown で、`b` / `e` / `h` / `k` の前ではなく、各子リスト終了後に空行が入るよう修正

```md
## test

- a
  - b
- c

1. d
   1. e
2. f

- g
  1. h
- i

1. j
   - k
2. l
```

期待される出力:
- 5 行目: `b`
- 6 行目: 空行
- 10 行目: `e`
- 11 行目: 空行
- 15 行目: `h`
- 16 行目: 空行
- 20 行目: `k`
- 21 行目: 空行

- ネストした子リスト直下の説明行を考慮した空行挿入を修正
  - 以下のような Markdown で、`c` の後に空行を入れ、`d` が次行ではなくその次の行に出力されるよう修正

```md
## test

- a
  - b
  c
- d
```

期待される出力:
- 5 行目: `b c`
- 6 行目: 空行
- 7 行目: `d`

- 引用ブロック内でも、ネストしたリスト終了後に浅い階層へ戻る際の自動空行ルールを適用するよう修正
- 引用ブロック内の字下げした説明行が、直前の引用内リスト項目セルへ正しく追記されない不具合を修正
- 以下のような Markdown で、`c` が別行へ出力されず、`b` のセルへ追記されるよう修正
- また、`g` の後に引用内空行が入り、`h` が次の引用項目として下の行へ出力されるよう修正

```md
## test

> - a
>   - b
>   c
> - d
> 
> - e
>   - f  
>   g
> - h
```

期待される出力:
- `c` は `b` と同じセルに追記される
  - 例: セル `E5` に `・ b c`
- `g` と `h` の間には引用内の空行が 1 行入る

### Changed
- `README.md` を更新
  - 現在のバージョン表記を `v2.2.4` に更新
  - リスト共通仕様を更新し、**ネストした子リスト終了後に浅い階層へ戻る際に自動空行を挿入する**挙動を明記
  - 引用ブロック内でも同ルールを適用することを明記
  - 引用内の字下げした説明行が、直前の引用内リスト項目セルへ追記される場合があることを明記

### Compatibility
- 後方互換性あり
- 設定項目の変更なし
- 入出力形式の変更なし

### Notes
- 本リリースは破壊的変更を含まないバグ修正リリース
- バージョンは `v2.2.3` から `v2.2.4` へ patch 更新

---

## [v2.2.3] - 2026-07-27

### Fixed
- 見出し直後にコードブロックが続く場合、コードブロック開始前の自動空行が挿入されず、コード本文の出力位置が 1 行上に詰まる不具合を修正
- 以下のような Markdown で、コード本文 `b` が 3 行目ではなく 4 行目に出力されるよう修正

````md
## a
```text
b
```
````

期待される出力:
- 2 行目: `a`
- 3 行目: 空行
- 4 行目: `b`

### Changed
- `README.md` の「コードブロック」説明を更新
  - 見出し直後にコードブロックが続く場合、開始前に 1 行の自動空行を挿入する仕様を明記

### Notes
- 本リリースは破壊的変更を含まないバグ修正リリース
- バージョンは `v2.2.2` から `v2.2.3` へ patch 更新

---

## [2.2.2] - 2026-07-26

### Fixed
- ネストしたリストの描画を修正
  - 番号付きリストの子が箇条書きリストの場合、子リスト開始前に1行空くよう修正
  - 番号付きリストの子が番号付きリストの場合、子リスト開始前に1行空くよう修正
  - 箇条書きリストの子が番号付きリストの場合、子リスト開始前に1行空くよう修正
  - 箇条書きリストの子が箇条書きリストの場合、子リスト開始前に1行空くよう修正
- 以下のような Markdown で、子リストが1行上に詰まって出力される問題を修正
  - `1. a` の下に `- b`
  - `1. a` の下に `1. b`
  - `- a` の下に `1. b`
  - `- a` の下に `- b`
- 引用ブロック内のネストリスト挙動は変更対象外とし、既存仕様を維持

### Compatibility
- 後方互換性あり
- 設定項目の変更なし
- 入出力形式の変更なし

---

## [2.2.1] - 2026-07-22

### Changed
- コードブロックの本文配置を見直し、本文をブロック開始位置の 1 列右に出力するよう変更
  - トップレベルコードブロック本文は **C 列起点**
- コードブロックの装飾位置を整理し、背景色と外周罫線を本文の 1 列左から適用するよう変更
  - トップレベルコードブロック装飾は **B 列起点**

### Fixed
- コードブロック本文が想定より左に出力され、トップレベルで **B 列** に入ってしまう不具合を修正
- コードブロック直前の Markdown 空行が再利用され、コード本文が 1 行上に詰まる不具合を修正
- 次のような入力で、コード本文 `b` が **B5** ではなく **C6** に出力されるよう修正

````md
## test

a

```text
b
```
````

### Docs
- README のコードブロック仕様を実装に合わせて更新
  - コード本文は **ブロック開始位置の 1 列右**
  - コードブロック装飾は **本文の 1 列左から**
  - コードブロック前の Markdown 空行は保持

---

## [2.2.0] - 2026-07-16

### Added
- 空行専用スタイルを追加
  - 空行フォントサイズを **6pt** に設定
  - Markdown 空行、自動空行、水平線行、引用内空行に適用
- 引用内空行専用の引用スタイルを追加
  - 引用の左罫線・背景を維持したまま空行フォントサイズを適用
- 出力開始位置オフセット対応を追加
  - 出力開始セルを **A1 から B2** に変更

### Changed
- 通常本文の既定フォントサイズを **10pt → 11pt** に変更
- 縦位置ダイアログの既定値を **下揃え** に変更
- 列幅の既定値を調整
  - POI 内部幅 `3.125 * 256`
  - Excel 表示上は **約 2.5**
- `mergeCols` の解釈を見直し
  - `mergeCols=40` の場合、列幅設定対象は **A〜AN**
  - 描画範囲は **B〜AM**
- 引用ブロックの配置を見直し
  - 引用本文は通常本文より **1 列右**
  - 引用の装飾（左罫線・塗りつぶし）は本文の **1 列左** から適用
- 引用ブロックの左罫線を **THICK** に変更
- 水平線スタイルを空行ベースに変更
  - 水平線行も 6pt ベースで描画
- README を現行仕様に合わせて更新

### Fixed
- 見出し、通常文、リスト、引用、コードブロック、水平線の **B2 起点レイアウト** を調整
- 見出し直後の自動空行が正しく入るよう修正
  - 箇条書き・番号付きリストの両方に対応
- リスト前後の空行再利用ルールを整理し、期待しない行詰めを修正
- 引用前後の空行処理を整理し、期待しない行詰め・余分な空行を修正
- 引用内の番号付きリスト / 箇条書き / 継続行の列配置を修正
- 引用内空行が通常フォントサイズで上書きされる問題を修正
- hard break（`<br>` / 行末2スペース / `\`）後の継続処理を改善
  - 通常文継続
  - 引用継続
  - リスト継続
  の状態遷移を修正
- 水平線前後の空行再利用を調整し、余分な空行が出るケースを修正
- `mergeCols` 指定時の右端列計算を修正し、列幅設定範囲と描画範囲のずれを解消

### Notes
- このバージョンでは、生成される Excel の **行位置・列位置・余白・引用装飾** が `v2.1.4` から変わります。
- 既存ファイルとの差分確認を推奨します。

---

## [2.1.4] - 2026-04-23

### 修正

- コードフェンスの終端判定を修正
  - 開始フェンスの記号種別と本数を保持し、閉じフェンスを「同一記号」「開始時以上の長さ」「後続は空白のみ」の条件で判定するよう改善
- 4連以上のバッククォートで囲まれたコードブロック内に含まれる3連バッククォートを、終端ではなく本文として正しく出力するよう修正
- コードブロック途中で終端誤判定が発生した場合に、背景色や罫線が一部の行へ適用されない問題を修正
- コードブロック全体に対して背景色・罫線が一貫して適用されるよう改善

### 互換性

- 既存の通常 Markdown 変換処理との後方互換性を維持
- 破壊的変更なし

---

## [2.1.3] - 2026-03-29

### Fixed
- 閉じていない強調記法（`*`, `_`, `**`, `__`）が、行内で不正に斜体・太字として解釈される場合がある不具合を修正しました。
- 次行継続を伴わない未閉じ強調記号が、文字として出力されず装飾状態だけが残ることがある問題を修正しました。
- `a*b`, `*c`, `_f`, `g**h`, `**i`, `__l` のような入力が、CommonMark に沿ってリテラル文字列として出力されるよう修正しました。

### Changed
- 未解決の強調開始記号を次行継続として扱うのは、行末のハード改行または `<br>` により継続が明示される場合のみに限定しました。

---

## [2.1.2] - 2026-03-11

### Fixed
- 改行をまたぐ強調記法の書式継続を修正しました。
  - `**bold**`
  - `*italic*`
  - `__bold__`
  - `_italic_`
- 複数行にまたがる強調記法が、各行で通常フォントとして出力されてしまう問題を修正しました。
- 以下のような Markdown で、各行の文字列部分に対して意図した太字・斜体が適用されるようにしました。

```md
## title

**a  
b**
```

### Changed

* 行末のハード改行を含むケースでも、開いている強調状態を次行へ適切に引き継ぐように改善しました。
* 行末 `<br>` を含むケースでも、強調記法の継続状態が正しく維持されるようにしました。

---

## [2.1.1] - 2026-03-10

### Fixed
- 箇条書きの継続行が同一セルに追記される場合があった不具合を修正。
- 継続行は縦方向の新規行として出力し、別セルに分かれるよう変更。

#### 例
```md
## title

* test  
  text  
  text
```

修正前:

* C4: `text text`

修正後:

* C4: `text`
* C5: `text`

---

## [2.1.0] - 2026-03-03

### Changed
- `*` / `_` の強調判定を、逐次トグル方式から CommonMark 寄りの **delimiter run 解決**方式へ変更
- delimiter run 判定で Unicode の whitespace / punctuation を考慮
- `_` の強調について追加条件（誤検出抑止）を反映
- delimiter run の **rule of 3** を反映
- `***` / `___` などの複合 run（太字＋斜体）に対応

### Fixed
- `<br>` による縦展開（改行分割）をまたぐ際、**開いている強調だけを閉じて**次行で再オープンするように修正（意図しない装飾崩れの抑止）

---

## [2.0.0] - 2026-03-01

### Added
- 箇条書き `- `（先頭が `-` + 空白の行）のレンダリングに対応（既存の `* ` と同等の扱い）
- 箇条書き `+ `（先頭が `+` + 空白の行）のレンダリングに対応（既存の `* ` / `- ` と同等の扱い）
- インライン強調の対応を拡張し、斜体 `*...*` / `_..._`、太字 `__...__`、太字＋斜体の組み合わせをサポート
- 打ち消し線 `~~...~~` のインライン装飾に対応
- 行末の「半角スペース2個以上」または「バックスラッシュ `\`」によるハード改行に対応

### Changed
- パッケージ構成を整理（例: `md2excel.app` / `md2excel.config` / `md2excel.excel` / `md2excel.markdown` / `md2excel.render`）
- エントリポイント（main）の完全修飾クラス名が変更
  - `md2excel.MarkdownToExcel` → `md2excel.app.MarkdownToExcel`
- パッケージ分割に伴い、外部参照が必要なクラス／メンバを `public` 化（例: `MdStyle` のスタイル、`RenderContext`、`MarkdownRenderer.render` など）
- 設定の読み込み方式を変更：**常にダイアログ（GUI）で設定**するように変更（CLI 引数は使用しない）
- 番号付きリスト判定を拡張し、`1. ` に加えて `1) ` 形式も認識するように変更
- 見出し判定を拡張し、ATX 見出しの行末の閉じ `#` を取り除いて見出しテキストとして扱うように変更（例: `## title ##`）
- 水平線判定を拡張し、`---` に加えて `***` / `___` / `- - -` / `* * *` など、空白・タブを含む 3 個以上の同一記号も水平線として認識するように変更
- フェンスコードブロック判定を拡張し、バッククォートフェンスに加えてチルダフェンス（`~~~`）も認識するように変更
- フェンスコードブロックの対応範囲を整理し、開始フェンス後ろの info string を許容する実装であることを明記
- インラインコード判定を拡張し、複数長のバッククォート列を使ったコードスパンを解釈するように変更
- `<br>` 分割時のインライン装飾の継続処理を拡張し、斜体・太字斜体・打ち消し線も保持するように変更
- テーブル判定を拡張し、先頭 / 末尾の `|` がない行でも、インラインコード外に `|` が 1 つ以上あればテーブル行として扱うように変更
- テーブルの行分割処理を変更し、先頭 / 末尾の `|` は存在する場合のみ除去するように変更

### Removed
- CLI 引数モード（`input.md [output.xlsx] [mergeCols] [fontName]`）による設定

### Notes
- 起動クラス名（FQCN）や import は新しいパッケージに合わせて更新してください
- 本バージョンでは、実行時に引数を渡しても設定には使用されません（常にダイアログが表示されます）
- 引用は従来どおり `trimmed.startsWith(">")` ベースで判定しているため、`>` の直後に空白がない形式も既に認識対象です（挙動変更なし）

---

## [1.0.0] - 2025-12-24

### Added
- Markdown（UTF-8）を Excel（.xlsx）に変換する基本機能（出力シート: `spec`）
- 見出し `#` / `##` / `###` / `####+` のスタイル出力
- 箇条書き `* ` と番号付きリスト `1. ` の出力（インデントに応じたネスト）
- 引用 `>` のブロック出力（左罫線＋背景）
- コードブロック（``` フェンス）の出力（背景＋外枠罫線、ASCII/CJK フォント切替）
- テーブル（`|...|`）の出力（ヘッダ/ボディの罫線・スタイル）
  - セル内 `\|` の復元、`` `...` `` 内の `|` を区切りにしない処理
- インライン装飾（セル内リッチテキスト）
  - 太字 `**...**`
  - インラインコード `` `...` ``（赤字、等幅、ASCII/CJK フォント切替）
- `<br>` の扱い
  - インラインコード外の `<br>` を改行として解釈し、縦方向展開
  - 行末 `<br>` による次行への継続（見出し／引用／リスト／通常文）
- 実行モード
  - CLI 引数モード: `input.md [output.xlsx] [mergeCols] [fontName]`
  - GUI 設定モード（引数なし）: ファイル選択、列数、フォント、縦位置、フォントサイズ設定

### Notes
- GUI（Swing）を使用するため、headless 環境では調整が必要な場合があります（完了ダイアログを含む）
- 箇条書きは `* ` のみ対応（`- ` / `+ ` は非対応）
- インライン Markdown は太字とインラインコードのみ対応（斜体・リンク等は未対応）
- コードブロックは ``` フェンスのみ対応

---